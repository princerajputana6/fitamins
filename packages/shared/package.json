{
  "name": "@fitamins/shared",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./deliverables": "./src/deliverables.js",
    "./validate": "./src/validate.js"
  },
  "scripts": {
    "lint": "echo \"(shared) no lint\" && exit 0",
    "build": "echo \"(shared) no build step\" && exit 0",
    "clean": "rm -rf .turbo"
  }
}
