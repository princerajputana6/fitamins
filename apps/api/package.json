{
  "name": "@fitamins/api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "lint": "echo \"(api) no lint\" && exit 0",
    "build": "echo \"(api) no build step\" && exit 0",
    "clean": "rm -rf .turbo"
  },
  "dependencies": {
    "@fitamins/shared": "*",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "mongoose": "^8.7.0",
    "resend": "^6.18.1"
  }
}
